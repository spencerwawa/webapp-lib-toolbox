# Local environment config — never commit secrets
.env

# Docker build cache artifacts
docker/gitnexus/.docker-build-cache/
.gitnexus
.claude/*
AGENTS.md
CLAUDE.md

# Corporate proxy certificates (generated locally — never commit)
certs/corporate-proxy.pem
